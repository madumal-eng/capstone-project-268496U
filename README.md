# Capstone Project

## Project Overview
[Add a short description of your capstone project here — the problem you're solving, the AI/ML approach you're using, and the expected outcome.]

## Folder Structure

capstone-project/
│
├── README.md              # Project documentation (this file)
├── requirements.txt       # Python dependencies required to run this project
├── .gitignore              # Files/folders excluded from version control
│
├── data/
│   ├── raw/                # Original, unmodified dataset(s). Treated as read-only.
│   └── processed/          # Cleaned and transformed data, ready for modeling
│
├── notebooks/              # Jupyter notebooks used for exploration, experimentation,
│                            # and prototyping (EDA, initial model testing, etc.)
│
├── src/                    # Source code for the core pipeline
│   ├── preprocess.py       # Scripts for data cleaning and feature engineering
│   ├── train.py            # Script to train the machine learning model
│   └── evaluate.py         # Script to evaluate model performance and generate metrics
│
├── models/                 # Saved/trained model files (e.g., .pkl, .h5, .pt)
│
└── reports/
    └── figures/            # Plots, charts, and visualizations used in the final report

## Setup Instructions
1. Clone the repository
   git clone <repo-url>
   cd capstone-project-268496U

2. Install dependencies
   pip install -r requirements.txt

3. Place the dataset inside data/raw/

## Usage
1. Run preprocessing:
   python src/preprocess.py

2. Train the model:
   python src/train.py

3. Evaluate the model:
   python src/evaluate.py

## Results
[Summarize key results, metrics, and findings here once available.]

## Author
[D.G.S.M.Shirantha / Index no.-268496U / Masters of datascience and artificial intelligence University Of Moratuwa.Department of computer science and engineering.]
